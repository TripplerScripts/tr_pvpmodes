import createElement from '../../functions/createElement'

const root = createElement({
  parent: 'body',
  id: 'root',
  className: 'fixed w-full h-full flex items-end justify-center bg-black'
})
export default root